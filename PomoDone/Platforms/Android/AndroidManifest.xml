<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<!-- Session-end notification (runtime-requested on Android 13+) -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<!-- Exact alarms: SCHEDULE_EXACT_ALARM covers API 31-32 (user-revocable);
	     USE_EXACT_ALARM is auto-granted to alarm/timer apps on API 33+ -->
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
	<!-- Short partial wake-lock around the self-played session-end ring so it
	     sounds and auto-stops reliably with the screen off / under Doze. -->
	<uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>